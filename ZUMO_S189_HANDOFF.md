# ZUMO — S189 HANDOFF (written at S188 close · paste at top of Session 189)

## READ THIS FIRST

**S188's work IS PUSHABLE and DJ verifies the push himself (standing ruling, S158).**
`git rm ZUMO_S188_HANDOFF.md` is part of that push and **`ZUMO_S189_HANDOFF.md` IS A NEW FILE** —
both are checkboxes in GitHub Desktop, which is where the deletion and the new file get missed.
If `__pycache__/` exists, delete it LAST; it regenerates on every gate run.

**`site_parity` IS OWED AFTER THIS PUSH AND WAS BANKED AT S188 OPEN.** Build floor is **10m57s**.
At S188 open it read PARITY twice, at 12m45s and 14m01s past the previous push — both outside
the window, so the repeat was believable. **Run it past the floor at S189 open, twice, and
believe the repeat.** Inside the window a PASS can be the OLD site agreeing with itself.

**DO NOT TRUST THIS DOCUMENT ABOUT WHAT IS SEATED. S188 DID NOT TOUCH THE §16 SEATING DEBT.**
S187 measured **26** unseated rules and S188 spent itself on `L05-01`/`L05-02` and on
`svg_layout_audit`. Beyond §16.32–§16.44, §16.46 and §16.51, these have no numbered body either:
**§16.14 · §16.18 · §16.19 · §16.20 · §16.21 · §16.23 · §16.24 · §16.26 · §16.27 · §16.28 ·
§16.29.** §16.14 was recorded as this exact debt at **S145** and has never been seated. Each
session widened the WINDOW rather than the FAMILY, which is §24.6c on a scope. **Run a heading
walk over the whole §16 family, not a range.** **§24.24 IS SEATED WITH A NUMBERED BODY** — S188
did not repeat the pattern, as S187 did not before it.

**§24.24 NEW — A VERSION WITH TWO HOMES IS BUMPED IN BOTH, AND THE HOME THE TOOL READS IS THE
ONE THAT IS NOT OBVIOUS.** S188 bumped four banks through `# Bank version:`, `--currency`
reported **0 unbumped**, and **§24.2 then failed on `ZUMO_QUIZ_L12.yaml`** naming the YAML
`bank_version:` field still at its old value. `--currency` reads the comment; `--status` reads
the FIELD. A bank bump is TWO edits and reads like one, and a single find-and-replace cannot
close it because the comment carries the `v` prefix and the field is bare (S153). **THE BIBLE
ITSELF HAS TWO CHANGELOG HOMES** — filing v8.183 in the header alone left `current_session()`
returning **187** with `build_worklist --check` green, because nothing had moved. Both homes now
carry byte-identical 5,439-character entries.

**80/80 gates** · `gate_payload_match` **PASS**, advisory unmoved at **635** · `retired_claims`
**CLEAN**, **12 REGISTERED** · `prose_canon` **0 new / 7 pinned / 0 orphan** · `quiz_bank
--check` **16 banks valid** · `callout_id` **1133/0** · `strip_inline --verify` **0 dead** ·
`build_css` **574 rules** · `image_audit` current · `build_worklist --check` current ·
`session_versions --check` agrees on every version, `--currency` **0 unbumped**.
**`byte_audit` IS OWED NOTHING: no payload changed and no executable line moved.**

---

# 1. WHAT S188 DID

## THE FIRST GPT ROWS TO CLOSE SINCE L04 — `L05-01` AND `L05-02`

**`L05-01`: DJ ruled MOVE, DO NOT DELETE, and the row was narrower than GPT described.**
GPT called §5.15 a full for-loop anatomy re-teach. Measured, the duplication is three things:
the push-up-coach refresher (which also asserted *starts counting at 1*, against the
zero-counting rule L04 §8A.6 teaches), `L05_GRAPHIC_5-04_for_anatomy.svg` (a second drawing of
L04's `4-06`), and a Quick Practice tracing `i < 4` where L04 §8A.6 already runs that lap table
on `i < 3`. All three are out.

**THE h4 THAT LOOKED LIKE THE DEFECT HELD THE BOOK'S ONLY DEFINITION OF THE TERM.**
`id="term-for-loop"` appears in `Lesson_05.html` and **in no other lesson** — L04 defines
fourteen key terms and *For Loop* is not among them. Deleting the block as GPT proposed would
have deleted the term from the book. **KEPT**, with the `drawBar()` trace, the `i--` countdown,
Worked Example #2, the for/while NOTE and the CHECKPOINT.

**GRAPHIC 5.4 IS RULED OUT, NOT DELETED.** `PLANNED_EXPECTED` 146 → **145** and the §21
coverage baseline 1,209 → **1,208**, each with a stated reason in `book_gates.py`, so
**outstanding held at 15 and did not fall like progress (S135)**. The `.svg` stays on disk,
unreferenced. The figure-index row came out and the tbody was restriped; **verified by
reconstruction (§24.23 part 3)** — row payloads compared with the `<tr>` tag stripped, exactly
one row lost, **zero rows added or altered**. **GRAPHIC 5.4 IS NOW A GAP IN THE NUMBERING AND
5.5/5.6/5.7 WERE NOT RENUMBERED**, on the F1/F2/F3 precedent: renumbering silently changes what
an existing citation points at.

**`L05-02`: DJ ruled OMIT ONLY `readAllSensors();`.** Step 4 handed over all six prototypes on
the THIRD staging of `'readAllSensors' was not declared` (L02 Step 7, L04 Step 5, here). Five
are now pasted and the sixth is the student's: the build stays red, the compiler names the
missing function, and **§5.5 still carries the full list** for checking after rather than
before. That is GPT's *first: explain / second: remind / third: expect* spiral implemented.

**A BANK EDIT WAS OWED AND READING IS WHAT FOUND IT.** `L05_A02`'s correct option and one
distractor `why` both asserted the six arrive in one trip. All eight §5.15-citing items were
read against the live section; every other one maps to retained content.

## THE DOUBLE CHECK AND THE TRIPLE CHECK BOTH FOUND DEFECTS PAST 80 GREEN GATES

**Double check (three):** (1) cutting the graphic left `<h4>For Loop Anatomy</h4>` standing over
nothing but the KEY TERM — a heading promising anatomy and delivering a definition;
(2) the bank fix introduced the **longest-option tell**, an 82-character correct answer against
distractors of 39, 32 and 43, reverted with the nuance moved into the wrong option's `why`
(structurally exempt from `retired_claims`' assertive register); (3) the CSS baseline note
written in the same pass recorded `.h4-c-6f7582-2` at 34 → 32 when the true figure was 34 → 31.

**Triple check (two, and one was a claim I had to withdraw):** LIVE.md's line-6 versions block
had been spliced BEFORE a later version bump and was stale at `svg_layout_audit v1.20` — the
exact drift `--check` exists to catch, reintroduced by the fixer. **And the S188 report that
"CONTROL B's seeds are tuned to one reference file" was half wrong**: the `badge anchor removed`
failure was a too-loose picker written in the same session, not a pre-existing flaw.
**Every one of the five was found by reading the artefact, not by an instrument.**

## `svg_layout_audit` v1.20 → **v1.21.2**

**IT NEVER READ `textLength`.** `grep textLength svg_layout_audit.py` returned nothing, so the
tool measured its own font estimate rather than the declared advance the renderer actually hits.
That produced three phantom overlaps in `L03_GRAPHIC_3-11_command_anatomy.svg`, whose four code
spans abut at **exactly 0.0px** once the attribute is honoured. **Twelve drawn SVGs declare
`textLength`.** Edge cases measured across all twelve before the change: **no** non-uniform
scale, **no** text-level `textLength` over positioned tspans, **no** unparseable values.

**IT ALSO GOT LOUDER, AND THAT HALF IS REAL.**
`L07_GRAPHIC_7-15_platformio_file_tree.svg` went 1 → **5** overflow findings, because the
estimate had been UNDER-reading. `RobotSensors.h` declares `textLength="140"` inside a group
scaled 1.2308, renders 172 units wide, and its ink ends **14.5 units past** the panel edge at
378 — the estimate put it at 375, just inside. **One of the five is a real defect worth fixing;
two more overflow by 2.2 units and are hairline.**

**THE DECLARED-WIDTH FLOOR (§24.19, DJ deferred the call).** The overflow floor is
`max(MIN_OVERFLOW, OVERFLOW_REL * width)`, and the relative term is an ERROR model — measured
font error runs to ~1.3%. **A declared width carries no error**, so declared rows now get the
absolute floor only. `MIN_OVERFLOW = 2.0` stays for them as a **VISIBILITY** floor, not an error
one. **MEASURED: THIS CHANGED ZERO FINDINGS.** The generated worklist before and after differs
only in the version stamp. **Do not credit it with the L07 rows** — they clear either floor.

**THE SELFTEST WAS VALIDATING AGAINST A POPULATION OF ONE.** `kill_anchor` and `collide`
hardcoded `callout-6` and `callout-7`; `widen` grepped the literal `'Lights whenever'`;
`collide` also wrote the fixed coordinates `x=150, y=262`; four photo seeds crashed on drawn
graphics; `photo credit removed` failed on any file with no photograph; `designer font first`
failed on any file with no `<text>`. **The `callout-6` crash was hiding all of it** — it died
before CONTROL B ran a single seed. Every seed is now structure-driven, and **a seed with no
structure to mutate reports `NOT RUN` instead of aborting**, with the verdict refusing to print
ALL CONTROLS PASS when anything was skipped. That is §16.44 applied to the selftest itself.

Measured across all **82** clean files in `images/`:

| | before the rework | after |
|---|---|---|
| ALL CONTROLS PASS | 3 | 3 |
| PASS with NOT RUN | — | 79 |
| FAILED | 17 | **0** |
| crashed | 62 | **0** |

**Every failure was the harness, not a file.** `widen` must seed a label **alone on its
baseline** — lengthening one with a neighbour makes it collide before it overflows, and the run
then reports `overlaps text`, a real finding but not the one the control proves.

**ONLY 3 OF 82 CLEAN FILES CARRY A NUMBERED BADGE**, so that control still has a population of
three. It now says so out loud rather than taking the sweep down with it. `L01_GRAPHIC_1-13_zumo_rear_view.svg`
is the pinned reference: **20 controls, ALL CONTROLS PASS.**

## ONE GRAPHIC FIXED, ON DJ'S RULING

`L01_GRAPHIC_1-19_playfrequency_anatomy.svg` had two real text-on-text collisions at y=163.
Five `x` values relaid onto the 18px Courier grid (`.` `playFrequency` `(` `440` `,`);
`800` / `,` / `15` / `);` untouched because **the cascade stops at the comma**; **no polygon
touched**, and both connectors still land inside their tokens. Audit reports it clean.
**DJ ruled S188: these are drawn vectors, not photographs — no reshoot was ever needed.**

---

# 2. S189 OPENS HERE

**THE GPT LIST IS THE ASSIGNMENT. DJ: *"I can't ship a book with errors in it."***
**DJ's S182 scope ruling stands: L01–L08 before L13–L16.**
**Fall launch is September 8.**

## THE IN-SCOPE REMAINDER
**L06 ×7 · L07 ×10 · L08 ×14**, and Part 0 now reads **66 closed / 177 open**.

## THE OBVIOUS NEXT MOVES
1. **L06 ×7.** These also close the **7 pinned `prose_canon` residue sites** — the three L05/L06
   lesson headings and four Maker labels are **ONE fix, not two**. S188 did not touch them;
   `prose_canon` still reads 0 new / 7 pinned / 0 orphan.
2. **APPEND TO THE REGISTRY WHEN YOU CLOSE A ROW.** One line in `retired_claims.REGISTRY`, and
   **measure the claim at zero in the assertive register first**. A wrong option's `why` is
   structurally excluded from that register — only the stem, and a correct option's `why`.
3. **ENUMERATE THE CLAIM, NOT THE ROW (§16.51).** Still the standing discipline.
4. **`prose_canon` arms 1, 2 and 4** — still unbuilt. **No arm without a control per direction
   (§16.50).**
5. **Seat the §16 debt.** 26 rules. One session could take a real bite.
6. **`L07_GRAPHIC_7-15`'s one real overflow** — `RobotSensors.h`, 14.5 units. The other two at
   2.2 units are hairline and are DJ's call.

## L01–L04 READINESS, RE-MEASURED AT S188
- **Zero open GPT rows in L01–L03** beyond two parked by DJ's own S178 ruling: **`L02-06`**
  (`GLOBAL VARIABLES` at 0 occurrences against a Challenge 3 that says *declare three counters
  up top*) and **`L02-09`** (the baud comment appears 4× inside L02's own payload, so a
  lesson-only fix breaks `gate_payload_match` — it is a Maker edit and rides with `L02-06`).
  **L02 ships Sept 8 with two known defects, by ruling, not by oversight.**
  `L02-07` is REFUTED (S154), `L02-12` and `L02-19` STRUCK (S180). `L03-B1` is HALF-CLOSED and
  its open half is bench row F11.
- **14 of the 18 open bench rows are L01–L03**: L01 ×8 (F1–F8), L02 ×2 (F9, F10), L03 ×4
  (F11–F14). **None can be closed from the repo.** **F10** (`L02-B2`) and **F14** (`L03-B3b`)
  carry the two falsifiable predictions — if either fails, a live paragraph comes out.
- **BUYING MATTE BLACK ELECTRICAL TAPE CLOSES `L04-B3` (F17) BY ITSELF** and unblocks F15, F16,
  F18 **and the L04 learner-mode build**, blocked on materials since **S51**.
- **Seven figures are unshot and render as visible placeholders** — L03 IMAGE 3.2 / 3.5 / 3.6 +
  VIDEO 3.1 · L04 IMAGE 4.1 / 4.3 + VIDEO 4.1. **L01 and L02 are clean.**
  **DJ ruled S187: KEEP THE PLACEHOLDERS.**
- **THE "FIVE GRAPHICS WITH STUDENT-VISIBLE PROBLEMS" CARRIED SINCE S187 WAS WRONG.** All three
  L02 IR files are **unreferenced**, and `Lesson_02.html` references no IR asset at all — they
  are staged on disk, not on the page. Of the two that were live, `L01_GRAPHIC_1-19` is fixed
  and `L03_GRAPHIC_3-11` needed nothing. **There is no live graphics defect in L01–L03.**
- The A-Star *"brain of your Zumo robot"* claim is **dead at 0 occurrences** in both L01 and
  L03. S162 killed it. Do not re-report it.

## STANDING, UNCHANGED
- **`gate_payload_match` IS NOT ONE OF THE GATES** (S137) and **TAKES ARGUMENTS** —
  `python3 gate_payload_match.py newproject.html lessons/*.html`.
- **`byte_audit` ARM 2 CANNOT SEE A FIGURE IN PROSE.**
- **A BIBLE SESSION BUMP IS A REGENERATION OBLIGATION** (S175) — fired at S188 and **measured
  rather than assumed**: `GPT_WORKLIST.md`'s whole diff was the stamp line, S187 → S188.
- **`--live` and `--handoff` PRINT, they do not WRITE** (§24.20). Splice their output; never
  hand-type it. **LIVE.md carries TWO `**Versions:**` lines** — the second is inside a
  historical per-session block and is correct as history. **Edit the current-region line only,
  and re-splice it AFTER the last version bump of the session, not before.**
- **LIVE.md NEEDS A `## WHAT SHIPPED IN S<n>` BLOCK OR §16.44 CANNOT RESOLVE THE CURRENT
  REGION.** Moving the header session without adding one fails the gate.
- **`quiz_bank.py` LIVES IN `quizzes/`, NOT THE REPO ROOT.**
- **Syllabus dates still `[TBD]`; the day-by-day period grid is unbuilt.**

# HARNESS — NOT IN THE REPO. RUN THE SCRIPT.

```
apt-get install -y gcc-avr avr-libc
sh harness_setup.sh                     # objects: 41  AND  core stderr: clean
python3 byte_audit.py --sizes           # ~4 min at 221 payloads
python3 byte_audit.py --selftest ; --check ; --discards
```
**`--selftest` CANNOT RUN BEFORE `--sizes`** — CONTROL K dies with a `KeyError`.

**STANDING CONTROLS (last reproduced S183; NOT re-run at S184–S188, none of which touched a
payload byte):** `11/after_step_1` **20,592** · `11/finished` **20,778** · `12/finished`
**24,790** · `12/c2_slipalarm` **21,334** · `13/finished` **25,248** · `14/finished` **26,002** ·
`15/finished` **28,406** · `16/finished` **28,626**.

# STANDING AUTHORITY — §24.17, §24.19, §24.21
**Decide and report; do not ask.** Carve-outs: facts about the ROOM · irreversible moves ·
RoboLore brand and course scope. **§24.19 is the tiebreaker** — what is best for student
learning.

---
<!-- VERSION BLOCK: emitted by session_versions.py --handoff. Never hand-typed. -->
Fresh-clone verified at **`7b9c0aa`**. Census **41,708**.
Bible **v8.183** · `BookComponentStandard` **v01.13.0** · Maker **v2.67.2** ·
`marks/` **41** · `icons/` **49** incl. LICENSE.
`ZUMO_Syllabus_WORKING.md` **v1.3**.

Instruments: `book_gates` **v1.74.1** · `lesson_inventory` **v1.3.5** ·
`gen_component` **v1.6.1** · `pill_sweep` **v1.1** · `gate_payload_match` **v1.9.3** ·
`build_family_map` **v1.6.6.3** · `callout_id` **v1.0** · `keyterm_prefix` **v1.0.1** · `build_mark_index` **v1.1.0** · `gen_bonus_banner` **v1.4.1** ·
`gen_part_banners` **v1.2** · `session_versions` **v1.32.1** · `fit_raster_svg` **v1.2** ·
`flatten_alpha` **v1.2** · `svg_layout_audit` **v1.21.2** · `site_parity` **v1.2.1** ·
`build_css` **v1.4.0** ·
`image_audit` **v1.3** ·
`strip_inline` **v1.2** ·
`build_worklist` **v1.2** ·
`prose_canon` **v1.1.0** ·
`retired_claims` **v1.0.1** ·
`regex_audit` **v1.0** ·
`byte_audit` **v1.9.1** ·
`build_palette` **v1.1** ·
`class_sweep` **v1.0** ·
`color_index` **v1.0** ·
`entity_sweep` **v1.0** ·
`font_stack_sweep` **v1.3.0** ·
`next_pointer` **v1.2** ·
`family_tag` **v1.2.1** ·
`glossary_convert` **v1.0** ·
`mark_wire` **v1.0.2** ·
`glyph_scan` **v1.1** ·
`title_feed` **v1.0** ·
`quiz_bank` **v1.6.1** ·
`timer.html` **v1.3.2** ·
`harness_setup.sh` **v1.1** ·
`pio_harness.sh` **v3.1** ·
`going_deeper` **v01.6.1**.

Lessons: L01 v03.32.1 · L02 v03.26.1 · L03 v03.47.0 · L04 v04.29.6 · L05 v04.30.0 · L06 v04.34.0 · L07 v04.31.8 · L08 v04.32.2 · L09 v05.27.5 · L10 v02.30.4 · L11 v02.31.1 · L12 v01.35.2 · L13 v02.39.0 · L14 v02.36.2 · L15 v02.32.0 · L16 v02.28.1.
